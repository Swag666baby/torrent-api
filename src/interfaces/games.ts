export default interface Game{
    id: number;
    title: string;
    magnet: string;
    page: number;
    repacker: string;
    fileSize: string;
    uploadDate: string;
    createAt: string;
    updateAt: string;
}